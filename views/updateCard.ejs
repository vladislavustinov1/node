<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/bootstrap.css" />
  </head>
  <body>
    <%- include ('./partials/header') %>
    <div class="container">
      <h1><%= title %></h1>
      <p>Заполните форму ниже чтобы изменить пост</p>
      <% let myPostData = post[0]; %>
      <form method="post" action="/updateCard/<%= myPostData.id %>">
        <div class="mb-3">
          <label for="z" class="form-label">Title</label>
          <input
            type="text"
            class="form-control"
            name="post[title]"
            id="z"
            aria-describedby="helpId"
            value="<%= post.title %>"
            placeholder="Title"
          />
        </div>
        <div class="mb-3">
          <label for="s" class="form-label">Текст заметки</label>
          <textarea class="form-control" name="post[content]" id="s" rows="3">
  <%= post.content %></textarea
          >
        </div>
        <button type="submit" value="Post" class="btn btn-primary">
          Submit
        </button>
      </form>
    </div>
  </body>
</html>
